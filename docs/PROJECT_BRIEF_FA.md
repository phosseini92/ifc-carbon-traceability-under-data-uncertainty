# تعریف پروژه پژوهشی IFC و کربن — نسخه ۱٫۰ (اجرا و ممیزی‌شده)

## به‌روزرسانی ۲۰۲۶/۰۹/۰۶ — اجرا و ممیزی کامل شد

مجموعه ویژگی‌های نسخه ۰٫۲ در یک محیط تمیز و pinned (Python 3.12.3،
IfcOpenShell 0.8.5) به‌طور کامل اجرا، تست و ممیزی شد و به‌عنوان **v1.0** منتشر
شده است. تمام ۲۴ تست (۲۳ تست برنامه‌ریزی‌شده + ۱ تست regression که در همین
ممیزی اضافه شد) پاس شدند؛ یک باگ reproducibility واقعی پیدا و رفع شد. جزئیات
کامل در `AUDIT_REPORT_V1_0.md` و `docs/VALIDATION_STATUS_V0_2.md` است. بخش
«معیار موفقیت نسخه ۰٫۲» در پایین همان‌طور که در ابتدا نوشته شده حفظ شده،
اما اکنون همه‌ی موارد آن محقق و با اجرای واقعی تأیید شده‌اند.

## عنوان

**Traceable IFC-to-Carbon Workflows under Data Uncertainty**

گردش‌کار قابل ردیابی از IFC به غربالگری کربن در شرایط عدم‌قطعیت داده

## مسئله پژوهش

وجود فایل IFC، مقدار مصالح و یک پایگاه داده محیط‌زیستی به‌تنهایی تضمین نمی‌کند
که نتیجه قابل اعتماد باشد. خطا ممکن است در هندسه، quantity، شناسه، نام مصالح،
فایل نگاشت یا انتخاب معنایی رکورد محیط‌زیستی رخ دهد.

نسخه ۰٫۲ سؤال را به سه سطح تقسیم می‌کند:

1. آیا داده IFC از نظر ساختاری کامل و سازگار است؟
2. آیا نگاشت فعال با رکوردی که قبلاً بازبینی و ثبت شده یکسان است؟
3. آیا آن رکورد واقعاً از نظر ماده، فناوری، جغرافیا و boundary انتخاب درستی است؟

پروژه نشان می‌دهد سطح سوم را نمی‌توان صرفاً با consistency نرم‌افزاری اثبات کرد.

## دامنه نمونه اصلی

پوسته کنترل‌شده یک اتاق با چهار دیوار آجری و یک دال بتنی است. سقف، بازشو، ملات،
آرماتور، نازک‌کاری و تأسیسات خارج از دامنه‌اند. این مدل ساختمان واقعی یا design
validation نیست.

محاسبه فقط از KBOB Fabrication برای رکوردهای منتخب استفاده می‌کند و whole-building
LCA یا EN 15804 A1-A3 ادعا نمی‌کند.

## آزمایش‌های کنترل‌شده

1. baseline کامل
2. حذف material assignment
3. ماده بدون mapping
4. volume ×1000
5. GUID تکراری
6. تغییر mapping بدون تغییر review contract
7. mapping معتبر اما اشتباه که review contract هم عمداً با آن هماهنگ شده است

آزمایش ششم باید توسط کنترل integrity متوقف شود. آزمایش هفتم عمداً باید نشان دهد
که semantic correctness با سازگاری داخلی یکی نیست.

## عدم‌قطعیت

۱۰ هزار نمونه پایه حفظ شده است. quantity، density و fabrication factor با بازه‌های
آموزشی شفاف perturb می‌شوند. علاوه بر اجرای ترکیبی، هر خانواده عدم‌قطعیت جداگانه
screen می‌شود تا مشخص شود کدام فرض‌ها دامنه خروجی را بیشتر تغییر می‌دهند.

این تحلیل variance decomposition رسمی یا uncertainty تجربی نیست.

## IFC مستقل

یک فایل `Slab.ifc` از پروژه عمومی IfcScript به‌عنوان fixture مستقل اضافه شده تا
workflow فقط مدل ساخته‌شده توسط همان کد را نخواند. این فایل material layer set دارد.
در نسخه ۰٫۲ لایه‌ها توصیف می‌شوند، اما چون allocation صریح لایه‌ها هنوز تعریف نشده،
carbon total برای آن آزاد نمی‌شود.

## معیار موفقیت نسخه ۰٫۲ (همه در v1.0 با اجرای واقعی محقق شدند)

- همه ۲۳ تست در محیط pinned پاس شوند. ← محقق شد؛ به‌علاوه ۱ تست جدید (مجموعاً ۲۴).
- baseline با محاسبه دستی همخوان باشد. ← محقق شد: ۴۰۰۱.۹۱۲۳ kg CO2e، دقیقاً یکسان.
- metre/mm یک نتیجه بدهند. ← محقق شد.
- چهار خطای ساختاری block شوند. ← محقق شد.
- mapping tamper با contract mismatch متوقف شود. ← محقق شد.
- approved-but-wrong mapping از کنترل خودکار عبور کند و به‌عنوان blind spot گزارش شود. ← محقق شد (تغییر ۳۴.۸٪ در نتیجه).
- IFC مستقل باز شود و layer assignment آن درست توصیف شود، بدون ادعای carbon validation. ← محقق شد.
- run manifest تمام ورودی‌ها، mapping contract و خروجی‌ها را hash کند. ← محقق شد.
- اجرای مجدد در پوشه تازه نتایج کلیدی را بازتولید کند. ← محقق شد و فراتر رفت: هر ۴۵ فایل خروجی در دو اجرای کاملاً مستقل بایت‌به‌بایت یکسان بودند (بعد از رفع یک باگ reproducibility که در همین ممیزی کشف شد — نگاه کنید به `AUDIT_REPORT_V1_0.md`).
