# از اینجا شروع کنید — نسخه ۱٫۰ (اجرا و ممیزی‌شده)

این پروژه اکنون فقط «محاسبه کربن از IFC» نیست. سؤال پژوهشی دقیق‌تر شده است:

**کدام خطاهای مسیر IFC → داده مصالح → محاسبه محیط‌زیستی را می‌توان با کنترل
خودکار تشخیص داد، کدام خطاها نیازمند قرارداد بازبینی و provenance هستند، و کدام
خطاهای معنایی حتی با وجود سازگاری کامل ساختاری می‌توانند از کنترل عبور کنند؟**

## مهم‌ترین ارتقای نسخه ۰٫۲

1. کنترل‌های ساختاری از کنترل «صحت نگاشت» جدا شده‌اند.
2. فایل `material_mapping_contract.json` اضافه شده تا تغییر ناخواسته نگاشت نسبت به
   رکورد و UUID بازبینی‌شده متوقف شود.
3. یک آزمایش جدید نشان می‌دهد اگر خود قرارداد بازبینی هم اشتباه باشد، نرم‌افزار
   نمی‌تواند معنای درست ماده را تضمین کند.
4. یک IFC مستقل از پروژه IfcScript برای آزمون خواندن convention متفاوت مصالح
   اضافه شده است؛ اما از آن نتیجه کربن معتبر ادعا نمی‌شود.
5. تحلیل عدم‌قطعیت اکنون علاوه بر اجرای ترکیبی، خانواده‌های quantity، density و
   factor را جداگانه هم screen می‌کند.
6. مجموعه تست پیشنهادی به ۲۳ تست گسترش یافته است.

## ترتیب مطالعه

1. `README.md`
2. `docs/METHODS.md`
3. `docs/RESEARCH_CLAIMS.md`
4. `docs/VALIDATION_STATUS_V0_2.md`
5. `workflow.py`
6. `run.py`
7. `tests/test_workflow.py`
8. `check_external_ifc.py`

## نکته بسیار مهم — به‌روزرسانی ۲۰۲۶/۰۹/۰۶

خروجی‌های پوشه `outputs_v0_1_verified` متعلق به اجرای تأییدشده نسخه ۰٫۱ هستند و
خروجی‌های `outputs_v1_0_verified` متعلق به اجرای تأییدشده نسخه ۱٫۰.

نسخه ۰٫۲ اکنون در یک محیط تمیز Python 3.12.3 با IfcOpenShell 0.8.5 به‌طور کامل
اجرا، تست (۲۴/۲۴ تست) و ممیزی شده و به‌عنوان **v1.0** منتشر شده است — دیگر
source-complete/execution-pending نیست. در همین ممیزی یک باگ reproducibility
واقعی (در دو فیکسچر آزمایشی) پیدا و رفع شد؛ جزئیات کامل در `AUDIT_REPORT_V1_0.md`.

قبل از استفاده از این پروژه در پرونده EPFL، باید بتوانید تفاوت این سه مفهوم را
با زبان خودتان توضیح دهید — این همچنان مهم‌ترین چیزی است که باید از پروژه یاد
بگیرید، فارغ از اینکه حالا اجرا شده است:

- structural integrity
- mapping integrity
- semantic validity

همین تفکیک، هسته پژوهشی قوی‌تر پروژه است.
